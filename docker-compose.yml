services:
  app:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - tm_data:/data
    environment:
      - ZOOM_WEBHOOK_SECRET_TOKEN=${ZOOM_WEBHOOK_SECRET_TOKEN}
      - ZOOM_CLIENT_ID=${ZOOM_CLIENT_ID}
      - ZOOM_CLIENT_SECRET=${ZOOM_CLIENT_SECRET}
      - TM_WHISPER_MODEL=base.en

volumes:
  tm_data:
